name: agent_cli
description: >-
  Discovers and drives already-installed AI coding CLIs (Claude Code, Codex,
  Gemini CLI) across the host and its WSL distributions.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.12.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
