.dart_tool/
.packages
pubspec.lock

# Fetched by tool/fetch_models.sh — hundreds of MB, never committed.
models/
native/

*.wav
*.raw
samples/
voice_clone_poc/.dart_tool/

# Generated, and it embeds absolute paths from whoever ran `pub get` last.
.flutter-plugins-dependencies
